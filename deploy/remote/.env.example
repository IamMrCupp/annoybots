# Copy to .env. The shared-hub Redis ADDRESS goes in bot.yaml (botnet.redis_addr);
# only secrets live here.

# Password for the shared hub Redis (matches the hub's --requirepass).
REDIS_PASSWORD=

# This bot's own tokens — whatever its networks in bot.yaml reference.
DISCORD_TOKEN=
# LIBERA_SASL=
