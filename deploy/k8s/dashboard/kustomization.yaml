apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
# Apply into the same namespace as the bots + Redis:
#   kubectl apply -k deploy/k8s/dashboard -n annoybots
resources:
  - dashboard.yaml
