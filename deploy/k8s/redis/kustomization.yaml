apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# Deploy the shared botnet bus ONCE (it is not per-bot, so no name prefix):
#   kubectl apply -k deploy/k8s/redis
resources:
  - redis.yaml
